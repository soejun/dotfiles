# Look and feel — gaps, borders, rounding, blur, shadows, animations.
# Tokyo Night Storm palette (matches config/kitty/themes/tokyonight/
# tokyonight_storm.conf and the Debian/Sway box).
# https://wiki.hypr.land/Configuring/Variables/

# Tokyo Night Storm accents
$active1  = rgba(7aa2f7ee)   # blue
$active2  = rgba(bb9af7ee)   # magenta
$inactive = rgba(414868aa)

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2

    col.active_border = $active1 $active2 45deg
    col.inactive_border = $inactive

    resize_on_border = true
    layout = dwindle
}

decoration {
    rounding = 10

    # Make the focused window pop.
    active_opacity = 1.0
    inactive_opacity = 0.92

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 6
        passes = 2
        new_optimizations = true
        vibrancy = 0.1696
    }
}

animations {
    enabled = yes

    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    animation = global,        1, 10,   default
    animation = border,        1, 5.39, easeOutQuint
    animation = windows,       1, 4.79, easeOutQuint
    animation = windowsIn,     1, 4.1,  easeOutQuint, popin 87%
    animation = windowsOut,    1, 1.49, linear,       popin 87%
    animation = fadeIn,        1, 1.73, almostLinear
    animation = fadeOut,       1, 1.46, almostLinear
    animation = fade,          1, 3.03, quick
    animation = layers,        1, 3.81, easeOutQuint
    animation = layersIn,      1, 4,    easeOutQuint, fade
    animation = layersOut,     1, 1.5,  linear,       fade
    animation = workspaces,    1, 1.94, almostLinear, fade
    animation = workspacesIn,  1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}
